import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  switchView: {
    display: 'flex',
    flexDirection: 'row',
  },
  switchElement: {
    borderRadius: 15,
  },
  headerContent: {
    alignItems: 'center',
  },
});
